//! HTTP-compatible error codes shared across all layers.
//!
//! This type is defined in `ironstar-core` and re-exported here for
//! backward compatibility with existing import paths.

pub use ironstar_core::error_code::ErrorCode;
