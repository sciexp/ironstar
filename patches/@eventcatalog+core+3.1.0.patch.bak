diff --git a/node_modules/@eventcatalog/core/dist/eventcatalog.js b/node_modules/@eventcatalog/core/dist/eventcatalog.js
index ac6d8eb..0f95fa0 100644
--- a/node_modules/@eventcatalog/core/dist/eventcatalog.js
+++ b/node_modules/@eventcatalog/core/dist/eventcatalog.js
@@ -156,7 +156,7 @@ program.command("dev").description("Run development server of EventCatalog").opt
       [
         {
           name: "astro",
-          command: process.platform === "win32" ? `npx astro dev ${command.args.join(" ").trim()} 2>&1 | findstr /V /C:"[glob-loader]" /C:"The collection" /C:"[router]"` : `npx astro dev ${command.args.join(" ").trim()} 2>&1 | grep -v -e "\\[glob-loader\\]" -e "The collection.*does not exist" -e "\\[router\\]"`,
+          command: process.platform === "win32" ? `bunx astro dev ${command.args.join(" ").trim()} 2>&1 | findstr /V /C:"[glob-loader]" /C:"The collection" /C:"[router]"` : `bunx astro dev ${command.args.join(" ").trim()} 2>&1 | grep -v -e "\\[glob-loader\\]" -e "The collection.*does not exist" -e "\\[router\\]"`,
           cwd: core,
           env: {
             PROJECT_DIR: dir,
@@ -204,8 +204,8 @@ program.command("build").description("Run build of EventCatalog").action(async (
   await runMigrations(dir);
   await catalogToAstro(dir, core);
   checkForUpdate();
-  const windowsCommand = `npx astro build ${command.args.join(" ").trim()} | findstr /V "The collection"`;
-  const unixCommand = `bash -c "set -o pipefail; npx astro build ${command.args.join(" ").trim()} 2>&1 | grep -v -e \\"\\\\[router\\\\]\\" -e \\"The collection.*does not exist\\""`;
+  const windowsCommand = `bunx astro build ${command.args.join(" ").trim()} | findstr /V "The collection"`;
+  const unixCommand = `bash -c "set -o pipefail; bunx astro build ${command.args.join(" ").trim()} 2>&1 | grep -v -e \\"\\\\[router\\\\]\\" -e \\"The collection.*does not exist\\""`;
   const buildCommand = process.platform === "win32" ? windowsCommand : unixCommand;
   execSync(
     `cross-env PROJECT_DIR='${dir}' CATALOG_DIR='${core}' ENABLE_EMBED=${canEmbedPages} EVENTCATALOG_STARTER=${isEventCatalogStarter} EVENTCATALOG_SCALE=${isEventCatalogScale} ${buildCommand}`,
@@ -222,7 +222,7 @@ var previewCatalog = ({
   isEventCatalogScale = false
 }) => {
   execSync(
-    `cross-env PROJECT_DIR='${dir}' CATALOG_DIR='${core}' ENABLE_EMBED=${canEmbedPages} EVENTCATALOG_STARTER=${isEventCatalogStarter} EVENTCATALOG_SCALE=${isEventCatalogScale} npx astro preview ${command.args.join(" ").trim()}`,
+    `cross-env PROJECT_DIR='${dir}' CATALOG_DIR='${core}' ENABLE_EMBED=${canEmbedPages} EVENTCATALOG_STARTER=${isEventCatalogStarter} EVENTCATALOG_SCALE=${isEventCatalogScale} bunx astro preview ${command.args.join(" ").trim()}`,
     {
       cwd: core,
       stdio: "inherit"
