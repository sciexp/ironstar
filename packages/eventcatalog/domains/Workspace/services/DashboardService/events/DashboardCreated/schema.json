{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "DashboardCreated",
	"type": "object",
	"title": "DashboardCreated",
	"description": "Recorded when a dashboard is created within a workspace.",
	"required": ["dashboardId", "workspaceId", "name", "timestamp"],
	"properties": {
		"dashboardId": {
			"type": "string",
			"description": "Unique identifier for the dashboard"
		},
		"workspaceId": {
			"type": "string",
			"description": "Unique identifier for the containing workspace"
		},
		"name": {
			"type": "string",
			"description": "Dashboard name (human-readable identifier within workspace)"
		},
		"timestamp": {
			"type": "string",
			"format": "date-time",
			"description": "Timestamp when the dashboard was created"
		}
	}
}
