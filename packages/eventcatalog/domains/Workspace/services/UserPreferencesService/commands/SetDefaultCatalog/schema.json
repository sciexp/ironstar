{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "SetDefaultCatalog",
	"type": "object",
	"title": "SetDefaultCatalog",
	"description": "Command to set a default catalog for automatic selection after login.",
	"required": ["catalogUri"],
	"properties": {
		"catalogUri": {
			"type": "string",
			"format": "uri",
			"description": "URI of the catalog to set as default"
		}
	}
}
