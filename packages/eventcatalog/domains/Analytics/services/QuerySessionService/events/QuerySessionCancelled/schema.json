{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "QuerySessionCancelled",
	"type": "object",
	"properties": {
		"queryId": {
			"type": "string",
			"format": "uuid",
			"description": "Unique identifier for this query execution"
		},
		"sessionId": {
			"type": "string",
			"format": "uuid",
			"description": "User session that initiated the query"
		},
		"cancelledAt": {
			"type": "string",
			"format": "date-time",
			"description": "Timestamp when cancellation was processed"
		},
		"cancelledBy": {
			"type": "string",
			"format": "uuid",
			"description": "User ID who cancelled the query"
		},
		"elapsedMs": {
			"type": "integer",
			"description": "Time elapsed before cancellation in milliseconds"
		}
	},
	"required": ["queryId", "sessionId", "cancelledAt", "cancelledBy"]
}
